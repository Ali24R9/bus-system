
<h1>Create a Station</h1>
<% @stations.each do |station| %>
  <%= link_to "#{station.station_name}", station_path(station) %></br>
<% end %>

<%= content_for(:title, "New Station | Bus Timetable") %>

<%= render './stations/form' %>

<p>----------------------------------------------------------------</p>



<h1>Create a Line</h1>
<% @lines.each do |line| %>
<%= link_to "#{line.line_number}", line_path(line) %></br>
<% end %>

<%= content_for(:title, "New line | Bus Timetable") %>

<%= render 'form' %>


