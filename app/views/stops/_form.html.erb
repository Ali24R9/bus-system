<%= form_for(@stop) do |f| %>
  <div class="form-group">
    <%= f.collection_select :line_id, Line.all, :id, :line_number %>
  </div>
  <div class="form-group">
    <%= f.collection_select :station_id, Station.all, :id, :station_name %>
  </div>
  <%= f.submit(:class => 'btn btn-succes') %>
 <% end %>


